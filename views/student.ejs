<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Student Dashboard</title>
  <style>
    * {
      box-sizing: border-box;
    }

    body {
      margin: 0;
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background-color: #f4f6f8;
      color: #333;
    }

    header {
      background-color: #2e86de;
      color: #fff;
      padding: 20px;
      text-align: center;
      font-size: 24px;
      font-weight: bold;
    }

    .container {
      display: flex;
      padding: 20px;
    }

    .sidebar {
      width: 220px;
      background-color: #fff;
      border-right: 1px solid #ddd;
      padding: 20px;
      height: 100vh;
      position: sticky;
      top: 0;
    }

    .sidebar h3 {
      font-size: 16px;
      color: #888;
      margin-bottom: 15px;
    }

    .sidebar a {
      display: block;
      text-decoration: none;
      padding: 10px 15px;
      margin-bottom: 10px;
      background-color: #eef3f7;
      color: #333;
      border-radius: 6px;
      font-weight: 500;
      transition: 0.2s ease;
    }

    .sidebar a:hover {
      background-color: #dce9f1;
    }

    .sidebar a.disabled {
      background-color: #f5f5f5;
      color: #aaa;
      pointer-events: none;
      cursor: not-allowed;
    }

    .main {
      flex: 1;
      padding: 20px 40px;
    }

    .welcome {
      font-size: 22px;
      font-weight: 600;
      margin-bottom: 10px;
    }

    .subtext {
      font-size: 14px;
      color: #666;
      margin-bottom: 30px;
    }

    .section {
      margin-bottom: 30px;
    }

    .section h3 {
      font-size: 18px;
      margin-bottom: 10px;
      color: #2c3e50;
    }

    .student-table {
      width: 100%;
      border-collapse: collapse;
      background: #fff;
      border-radius: 8px;
      overflow: hidden;
      box-shadow: 0 2px 8px rgba(0,0,0,0.05);
    }

    .student-table th, .student-table td {
      padding: 12px 16px;
      border-bottom: 1px solid #eee;
      text-align: left;
    }

    .student-table th {
      background-color: #ecf0f1;
      color: #333;
    }

    .student-table tr:last-child td {
      border-bottom: none;
    }

    @media (max-width: 768px) {
      .container {
        flex-direction: column;
      }

      .sidebar {
        width: 100%;
        height: auto;
        border-right: none;
        border-bottom: 1px solid #ddd;
      }

      .main {
        padding: 20px;
      }
    }
  </style>
</head>
<body>

  <header>
    Student Dashboard
  </header>

  <div class="container">
    <div class="sidebar">
      <h3>Navigation</h3>
      <a href="/user/principal" title="Access Denied">View Principal Dashboard</a>
      <a href="/user/teacher" title="Access Denied">View Teacher Dashboard</a>
    </div>

    <div class="main">
      <div class="welcome">Welcome, Student</div>
      <div class="subtext">This is your dashboard. You can view student-related info. Access to other dashboards is restricted.</div>

      <div class="section">
        <h3>School-wide Student List</h3>
        <table class="student-table">
          <thead>
            <tr>
              <th>Name</th>
              <th>Grade</th>
              <th>Email</th>
            </tr>
          </thead>
          <tbody>
            <% for(let i = 1; i <= 350; i++) { %>
              <tr>
                <td>Student <%= i %></td>
                <td><%= ['Grade 9', 'Grade 10', 'Grade 11', 'Grade 12'][i % 4] %></td>
                <td>student<%= i %>@school.edu</td>
              </tr>
            <% } %>
          </tbody>
        </table>
      </div>
    </div>
  </div>

</body>
</html>
